services:
  db:
    image: postgres:16-alpine
    container_name: poc-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: poc_user
      POSTGRES_PASSWORD: poc_recordweb
      POSTGRES_DB: poc_db
    volumes:
      - poc_db_data:/var/lib/postgresql/data
    networks:
      - poc_network

  api:
    build:
      context: .
      dockerfile: apps/fragenmanagement/backend/Dockerfile
    container_name: poc-api
    restart: unless-stopped
    depends_on:
      - db
    environment:
      PGHOST: db
      PGUSER: poc_user
      PGPASSWORD: poc_recordweb
      PGDATABASE: poc_db
      PUBLIC_BASE_URL: https://vps.recordweb.dev
      LDN_INBOX_URL: http://antwort-api:3000/antwortmanagement/api/inbox
    volumes:
      - ./schemas:/app/schemas:ro
    networks:
      - poc_network

  antwort-api:
    build:
      context: .
      dockerfile: apps/antwortmanagement/backend/Dockerfile
    container_name: poc-antwort-api
    restart: unless-stopped
    depends_on:
      - db
    environment:
      PGHOST: db
      PGUSER: poc_user
      PGPASSWORD: poc_recordweb
      PGDATABASE: poc_db
      PUBLIC_BASE_URL: https://vps.recordweb.dev
    volumes:
      - ./schemas:/app/schemas:ro
    networks:
      - poc_network

  resolver-parlament:
    build:
      context: ./apps/resolver
      dockerfile: backend/Dockerfile
    container_name: poc-resolver-parlament
    restart: unless-stopped
    environment:
      PORT: 3000
    volumes:
      - ./apps/resolver/parlament/org.json:/app/org.json:ro
      - resolver_parlament_data:/app/data
    networks:
      - poc_network

  resolver-bk:
    build:
      context: ./apps/resolver
      dockerfile: backend/Dockerfile
    container_name: poc-resolver-bk
    restart: unless-stopped
    environment:
      PORT: 3000
    volumes:
      - ./apps/resolver/bk/org.json:/app/org.json:ro
      - resolver_bk_data:/app/data
    networks:
      - poc_network

  sox:
    build:
      context: ./apps/sox/backend
    environment:
      PORT: 3000
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: poc_db
      POSTGRES_USER: poc_user
      POSTGRES_PASSWORD: poc_recordweb
      SOX_DID_NAMESPACE: s73f42a3
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped


  nginx:
    image: nginx:alpine
    container_name: poc-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./apps:/usr/share/nginx/html/apps:ro
    networks:
      - poc_network
    depends_on:
      - resolver-parlament
      - resolver-bk
      
networks:
  poc_network:
    driver: bridge

volumes:
  poc_db_data:
  resolver_parlament_data:
  resolver_bk_data: